source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick" # required by ruby >= 3.0 for `jekyll serve`

# Stdlib gems extracted from default in Ruby 4.x — needed by Jekyll 3.9
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"

group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-seo-tag"
  gem "jekyll-feed"
end
